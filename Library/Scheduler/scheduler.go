package Scheduler

func (r Scheduler) SchedulerStart() {
	// jakartaTime, _ := time.LoadLocation("Asia/Jakarta")
	// s := gocron.NewScheduler(jakartaTime)
	// s.Every(1).Day().At("00:00").Do(func() {
	// 	SchedulerResetLeaveBalance()
	// })
	// s.StartAsync()
	// log.Info().Msg("Scheduler start ....")
}
